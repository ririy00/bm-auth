# Auth Database Package

Prisma schema, migrations, and generated client for `auth-service`.

## Paths

- `prisma/schema.prisma`
- `prisma/migrations/`
- `generated/client/`

## Commands

Run from the repository root:

```bash
npm run prisma:generate:auth
npm run prisma:migrate:auth
npm run prisma:studio:auth
```
